/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { USERACCOUNT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const UPDATE_USER_DISCRIMINATOR = new Uint8Array([
  9, 2, 160, 169, 118, 12, 207, 84,
]);

export function getUpdateUserDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_USER_DISCRIMINATOR);
}

export type UpdateUserInstruction<
  TProgram extends string = typeof USERACCOUNT_PROGRAM_ADDRESS,
  TAccountUser extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountTreasury extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableAccount<TAccountUser>
        : TAccountUser,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateUserInstructionData = {
  discriminator: ReadonlyUint8Array;
  description: ReadonlyUint8Array;
};

export type UpdateUserInstructionDataArgs = { description: ReadonlyUint8Array };

export function getUpdateUserInstructionDataEncoder(): Encoder<UpdateUserInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['description', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_USER_DISCRIMINATOR })
  );
}

export function getUpdateUserInstructionDataDecoder(): Decoder<UpdateUserInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['description', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getUpdateUserInstructionDataCodec(): Codec<
  UpdateUserInstructionDataArgs,
  UpdateUserInstructionData
> {
  return combineCodec(
    getUpdateUserInstructionDataEncoder(),
    getUpdateUserInstructionDataDecoder()
  );
}

export type UpdateUserAsyncInput<
  TAccountUser extends string = string,
  TAccountAuthority extends string = string,
  TAccountTreasury extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user?: Address<TAccountUser>;
  authority: TransactionSigner<TAccountAuthority>;
  treasury: Address<TAccountTreasury>;
  systemProgram?: Address<TAccountSystemProgram>;
  description: UpdateUserInstructionDataArgs['description'];
};

export async function getUpdateUserInstructionAsync<
  TAccountUser extends string,
  TAccountAuthority extends string,
  TAccountTreasury extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof USERACCOUNT_PROGRAM_ADDRESS,
>(
  input: UpdateUserAsyncInput<
    TAccountUser,
    TAccountAuthority,
    TAccountTreasury,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateUserInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountAuthority,
    TAccountTreasury,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? USERACCOUNT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.user.value) {
    accounts.user.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([117, 115, 101, 114])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getUpdateUserInstructionDataEncoder().encode(
      args as UpdateUserInstructionDataArgs
    ),
  } as UpdateUserInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountAuthority,
    TAccountTreasury,
    TAccountSystemProgram
  >;

  return instruction;
}

export type UpdateUserInput<
  TAccountUser extends string = string,
  TAccountAuthority extends string = string,
  TAccountTreasury extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: Address<TAccountUser>;
  authority: TransactionSigner<TAccountAuthority>;
  treasury: Address<TAccountTreasury>;
  systemProgram?: Address<TAccountSystemProgram>;
  description: UpdateUserInstructionDataArgs['description'];
};

export function getUpdateUserInstruction<
  TAccountUser extends string,
  TAccountAuthority extends string,
  TAccountTreasury extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof USERACCOUNT_PROGRAM_ADDRESS,
>(
  input: UpdateUserInput<
    TAccountUser,
    TAccountAuthority,
    TAccountTreasury,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateUserInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountAuthority,
  TAccountTreasury,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? USERACCOUNT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.treasury),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getUpdateUserInstructionDataEncoder().encode(
      args as UpdateUserInstructionDataArgs
    ),
  } as UpdateUserInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountAuthority,
    TAccountTreasury,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedUpdateUserInstruction<
  TProgram extends string = typeof USERACCOUNT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    authority: TAccountMetas[1];
    treasury: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: UpdateUserInstructionData;
};

export function parseUpdateUserInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedUpdateUserInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      authority: getNextAccount(),
      treasury: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateUserInstructionDataDecoder().decode(instruction.data),
  };
}
