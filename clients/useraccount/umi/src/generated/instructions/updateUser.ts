/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u32,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateUserInstructionAccounts = {
  user?: PublicKey | Pda;
  authority?: Signer;
  treasury: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type UpdateUserInstructionData = {
  discriminator: Uint8Array;
  description: Uint8Array;
};

export type UpdateUserInstructionDataArgs = { description: Uint8Array };

export function getUpdateUserInstructionDataSerializer(): Serializer<
  UpdateUserInstructionDataArgs,
  UpdateUserInstructionData
> {
  return mapSerializer<
    UpdateUserInstructionDataArgs,
    any,
    UpdateUserInstructionData
  >(
    struct<UpdateUserInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['description', bytes({ size: u32() })],
      ],
      { description: 'UpdateUserInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([9, 2, 160, 169, 118, 12, 207, 84]),
    })
  ) as Serializer<UpdateUserInstructionDataArgs, UpdateUserInstructionData>;
}

// Args.
export type UpdateUserInstructionArgs = UpdateUserInstructionDataArgs;

// Instruction.
export function updateUser(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdateUserInstructionAccounts & UpdateUserInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'useraccount',
    'KoNT93eGXFcQJxZQqBvrBLQQCcxEPhKrmZoqFNmxGjk'
  );

  // Accounts.
  const resolvedAccounts = {
    user: { index: 0, isWritable: true as boolean, value: input.user ?? null },
    authority: {
      index: 1,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    treasury: {
      index: 2,
      isWritable: true as boolean,
      value: input.treasury ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateUserInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.user.value) {
    resolvedAccounts.user.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([117, 115, 101, 114])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateUserInstructionDataSerializer().serialize(
    resolvedArgs as UpdateUserInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
