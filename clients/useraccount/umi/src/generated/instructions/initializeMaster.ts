/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeMasterInstructionAccounts = {
  master?: PublicKey | Pda;
  authority?: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeMasterInstructionData = {
  discriminator: Uint8Array;
  feeRate: number;
  minFee: bigint;
  maxFee: bigint;
  monthlyPrice: bigint;
};

export type InitializeMasterInstructionDataArgs = {
  feeRate: number;
  minFee: number | bigint;
  maxFee: number | bigint;
  monthlyPrice: number | bigint;
};

export function getInitializeMasterInstructionDataSerializer(): Serializer<
  InitializeMasterInstructionDataArgs,
  InitializeMasterInstructionData
> {
  return mapSerializer<
    InitializeMasterInstructionDataArgs,
    any,
    InitializeMasterInstructionData
  >(
    struct<InitializeMasterInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['feeRate', u8()],
        ['minFee', u64()],
        ['maxFee', u64()],
        ['monthlyPrice', u64()],
      ],
      { description: 'InitializeMasterInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([206, 91, 246, 30, 216, 101, 134, 166]),
    })
  ) as Serializer<
    InitializeMasterInstructionDataArgs,
    InitializeMasterInstructionData
  >;
}

// Args.
export type InitializeMasterInstructionArgs =
  InitializeMasterInstructionDataArgs;

// Instruction.
export function initializeMaster(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: InitializeMasterInstructionAccounts & InitializeMasterInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'useraccount',
    'KoNT93eGXFcQJxZQqBvrBLQQCcxEPhKrmZoqFNmxGjk'
  );

  // Accounts.
  const resolvedAccounts = {
    master: {
      index: 0,
      isWritable: true as boolean,
      value: input.master ?? null,
    },
    authority: {
      index: 1,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeMasterInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.master.value) {
    resolvedAccounts.master.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 115, 116, 101, 114])),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeMasterInstructionDataSerializer().serialize(
    resolvedArgs as InitializeMasterInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
