/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import { findProgramStatePda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DepositToEscrowInstructionAccounts = {
  user: PublicKey | Pda;
  escrow?: PublicKey | Pda;
  /** Brukes for å hente månedspris */
  master?: PublicKey | Pda;
  programState?: PublicKey | Pda;
  authority?: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type DepositToEscrowInstructionData = {
  discriminator: Uint8Array;
  amount: bigint;
};

export type DepositToEscrowInstructionDataArgs = { amount: number | bigint };

export function getDepositToEscrowInstructionDataSerializer(): Serializer<
  DepositToEscrowInstructionDataArgs,
  DepositToEscrowInstructionData
> {
  return mapSerializer<
    DepositToEscrowInstructionDataArgs,
    any,
    DepositToEscrowInstructionData
  >(
    struct<DepositToEscrowInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['amount', u64()],
      ],
      { description: 'DepositToEscrowInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([246, 134, 57, 199, 116, 101, 68, 224]),
    })
  ) as Serializer<
    DepositToEscrowInstructionDataArgs,
    DepositToEscrowInstructionData
  >;
}

// Args.
export type DepositToEscrowInstructionArgs = DepositToEscrowInstructionDataArgs;

// Instruction.
export function depositToEscrow(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: DepositToEscrowInstructionAccounts & DepositToEscrowInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'useraccount',
    'KoNT93eGXFcQJxZQqBvrBLQQCcxEPhKrmZoqFNmxGjk'
  );

  // Accounts.
  const resolvedAccounts = {
    user: { index: 0, isWritable: true as boolean, value: input.user ?? null },
    escrow: {
      index: 1,
      isWritable: true as boolean,
      value: input.escrow ?? null,
    },
    master: {
      index: 2,
      isWritable: false as boolean,
      value: input.master ?? null,
    },
    programState: {
      index: 3,
      isWritable: true as boolean,
      value: input.programState ?? null,
    },
    authority: {
      index: 4,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DepositToEscrowInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.escrow.value) {
    resolvedAccounts.escrow.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([101, 115, 99, 114, 111, 119])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.user.value)
      ),
    ]);
  }
  if (!resolvedAccounts.master.value) {
    resolvedAccounts.master.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 115, 116, 101, 114])),
    ]);
  }
  if (!resolvedAccounts.programState.value) {
    resolvedAccounts.programState.value = findProgramStatePda(context);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDepositToEscrowInstructionDataSerializer().serialize(
    resolvedArgs as DepositToEscrowInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
