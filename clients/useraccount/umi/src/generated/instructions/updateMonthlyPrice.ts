/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateMonthlyPriceInstructionAccounts = {
  master?: PublicKey | Pda;
  authority?: Signer;
};

// Data.
export type UpdateMonthlyPriceInstructionData = {
  discriminator: Uint8Array;
  newMonthlyPrice: bigint;
};

export type UpdateMonthlyPriceInstructionDataArgs = {
  newMonthlyPrice: number | bigint;
};

export function getUpdateMonthlyPriceInstructionDataSerializer(): Serializer<
  UpdateMonthlyPriceInstructionDataArgs,
  UpdateMonthlyPriceInstructionData
> {
  return mapSerializer<
    UpdateMonthlyPriceInstructionDataArgs,
    any,
    UpdateMonthlyPriceInstructionData
  >(
    struct<UpdateMonthlyPriceInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['newMonthlyPrice', u64()],
      ],
      { description: 'UpdateMonthlyPriceInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([208, 197, 8, 227, 250, 154, 252, 129]),
    })
  ) as Serializer<
    UpdateMonthlyPriceInstructionDataArgs,
    UpdateMonthlyPriceInstructionData
  >;
}

// Args.
export type UpdateMonthlyPriceInstructionArgs =
  UpdateMonthlyPriceInstructionDataArgs;

// Instruction.
export function updateMonthlyPrice(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdateMonthlyPriceInstructionAccounts &
    UpdateMonthlyPriceInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'useraccount',
    'KoNT93eGXFcQJxZQqBvrBLQQCcxEPhKrmZoqFNmxGjk'
  );

  // Accounts.
  const resolvedAccounts = {
    master: {
      index: 0,
      isWritable: true as boolean,
      value: input.master ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateMonthlyPriceInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.master.value) {
    resolvedAccounts.master.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 115, 116, 101, 114])),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateMonthlyPriceInstructionDataSerializer().serialize(
    resolvedArgs as UpdateMonthlyPriceInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
