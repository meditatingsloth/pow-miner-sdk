/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type WithdrawFeeProgramFundsInstructionAccounts = {
  master?: PublicKey | Pda;
  masterSigner: Signer;
  feeProgram?: PublicKey | Pda;
  recipient: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type WithdrawFeeProgramFundsInstructionData = {
  discriminator: Uint8Array;
  amount: bigint;
};

export type WithdrawFeeProgramFundsInstructionDataArgs = {
  amount: number | bigint;
};

export function getWithdrawFeeProgramFundsInstructionDataSerializer(): Serializer<
  WithdrawFeeProgramFundsInstructionDataArgs,
  WithdrawFeeProgramFundsInstructionData
> {
  return mapSerializer<
    WithdrawFeeProgramFundsInstructionDataArgs,
    any,
    WithdrawFeeProgramFundsInstructionData
  >(
    struct<WithdrawFeeProgramFundsInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['amount', u64()],
      ],
      { description: 'WithdrawFeeProgramFundsInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([165, 147, 189, 246, 132, 25, 74, 194]),
    })
  ) as Serializer<
    WithdrawFeeProgramFundsInstructionDataArgs,
    WithdrawFeeProgramFundsInstructionData
  >;
}

// Args.
export type WithdrawFeeProgramFundsInstructionArgs =
  WithdrawFeeProgramFundsInstructionDataArgs;

// Instruction.
export function withdrawFeeProgramFunds(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: WithdrawFeeProgramFundsInstructionAccounts &
    WithdrawFeeProgramFundsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'useraccount',
    'KoNT93eGXFcQJxZQqBvrBLQQCcxEPhKrmZoqFNmxGjk'
  );

  // Accounts.
  const resolvedAccounts = {
    master: {
      index: 0,
      isWritable: true as boolean,
      value: input.master ?? null,
    },
    masterSigner: {
      index: 1,
      isWritable: false as boolean,
      value: input.masterSigner ?? null,
    },
    feeProgram: {
      index: 2,
      isWritable: true as boolean,
      value: input.feeProgram ?? null,
    },
    recipient: {
      index: 3,
      isWritable: true as boolean,
      value: input.recipient ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: WithdrawFeeProgramFundsInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.master.value) {
    resolvedAccounts.master.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 115, 116, 101, 114])),
    ]);
  }
  if (!resolvedAccounts.feeProgram.value) {
    resolvedAccounts.feeProgram.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([102, 101, 101, 95, 112, 114, 111, 103, 114, 97, 109])
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getWithdrawFeeProgramFundsInstructionDataSerializer().serialize(
    resolvedArgs as WithdrawFeeProgramFundsInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
