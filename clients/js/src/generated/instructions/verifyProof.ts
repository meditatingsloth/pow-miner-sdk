/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { findConfigPda, findProofPda } from '../pdas';
import { POW_MINER_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const VERIFY_PROOF_DISCRIMINATOR = new Uint8Array([
  217, 211, 191, 110, 144, 13, 186, 98,
]);

export function getVerifyProofDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_PROOF_DISCRIMINATOR
  );
}

export type VerifyProofInstruction<
  TProgram extends string = typeof POW_MINER_PROGRAM_ADDRESS,
  TAccountUser extends string | IAccountMeta<string> = string,
  TAccountUserAccount extends string | IAccountMeta<string> = string,
  TAccountProof extends string | IAccountMeta<string> = string,
  TAccountConfig extends string | IAccountMeta<string> = string,
  TAccountFeeReceiver extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & IAccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserAccount extends string
        ? ReadonlyAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountProof extends string
        ? WritableAccount<TAccountProof>
        : TAccountProof,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountFeeReceiver extends string
        ? WritableAccount<TAccountFeeReceiver>
        : TAccountFeeReceiver,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyProofInstructionData = {
  discriminator: ReadonlyUint8Array;
  slot: bigint;
  nonce: bigint;
  hash: ReadonlyUint8Array;
  totalHashes: bigint;
};

export type VerifyProofInstructionDataArgs = {
  slot: number | bigint;
  nonce: number | bigint;
  hash: ReadonlyUint8Array;
  totalHashes: number | bigint;
};

export function getVerifyProofInstructionDataEncoder(): Encoder<VerifyProofInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['slot', getU64Encoder()],
      ['nonce', getU64Encoder()],
      ['hash', fixEncoderSize(getBytesEncoder(), 32)],
      ['totalHashes', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VERIFY_PROOF_DISCRIMINATOR })
  );
}

export function getVerifyProofInstructionDataDecoder(): Decoder<VerifyProofInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['slot', getU64Decoder()],
    ['nonce', getU64Decoder()],
    ['hash', fixDecoderSize(getBytesDecoder(), 32)],
    ['totalHashes', getU64Decoder()],
  ]);
}

export function getVerifyProofInstructionDataCodec(): Codec<
  VerifyProofInstructionDataArgs,
  VerifyProofInstructionData
> {
  return combineCodec(
    getVerifyProofInstructionDataEncoder(),
    getVerifyProofInstructionDataDecoder()
  );
}

export type VerifyProofAsyncInput<
  TAccountUser extends string = string,
  TAccountUserAccount extends string = string,
  TAccountProof extends string = string,
  TAccountConfig extends string = string,
  TAccountFeeReceiver extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userAccount: Address<TAccountUserAccount>;
  proof?: Address<TAccountProof>;
  config?: Address<TAccountConfig>;
  feeReceiver: Address<TAccountFeeReceiver>;
  systemProgram?: Address<TAccountSystemProgram>;
  slot: VerifyProofInstructionDataArgs['slot'];
  nonce: VerifyProofInstructionDataArgs['nonce'];
  hash: VerifyProofInstructionDataArgs['hash'];
  totalHashes: VerifyProofInstructionDataArgs['totalHashes'];
};

export async function getVerifyProofInstructionAsync<
  TAccountUser extends string,
  TAccountUserAccount extends string,
  TAccountProof extends string,
  TAccountConfig extends string,
  TAccountFeeReceiver extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof POW_MINER_PROGRAM_ADDRESS,
>(
  input: VerifyProofAsyncInput<
    TAccountUser,
    TAccountUserAccount,
    TAccountProof,
    TAccountConfig,
    TAccountFeeReceiver,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  VerifyProofInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserAccount,
    TAccountProof,
    TAccountConfig,
    TAccountFeeReceiver,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? POW_MINER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userAccount: { value: input.userAccount ?? null, isWritable: false },
    proof: { value: input.proof ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    feeReceiver: { value: input.feeReceiver ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.proof.value) {
    accounts.proof.value = await findProofPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.proof),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.feeReceiver),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getVerifyProofInstructionDataEncoder().encode(
      args as VerifyProofInstructionDataArgs
    ),
  } as VerifyProofInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserAccount,
    TAccountProof,
    TAccountConfig,
    TAccountFeeReceiver,
    TAccountSystemProgram
  >;

  return instruction;
}

export type VerifyProofInput<
  TAccountUser extends string = string,
  TAccountUserAccount extends string = string,
  TAccountProof extends string = string,
  TAccountConfig extends string = string,
  TAccountFeeReceiver extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userAccount: Address<TAccountUserAccount>;
  proof: Address<TAccountProof>;
  config: Address<TAccountConfig>;
  feeReceiver: Address<TAccountFeeReceiver>;
  systemProgram?: Address<TAccountSystemProgram>;
  slot: VerifyProofInstructionDataArgs['slot'];
  nonce: VerifyProofInstructionDataArgs['nonce'];
  hash: VerifyProofInstructionDataArgs['hash'];
  totalHashes: VerifyProofInstructionDataArgs['totalHashes'];
};

export function getVerifyProofInstruction<
  TAccountUser extends string,
  TAccountUserAccount extends string,
  TAccountProof extends string,
  TAccountConfig extends string,
  TAccountFeeReceiver extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof POW_MINER_PROGRAM_ADDRESS,
>(
  input: VerifyProofInput<
    TAccountUser,
    TAccountUserAccount,
    TAccountProof,
    TAccountConfig,
    TAccountFeeReceiver,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): VerifyProofInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserAccount,
  TAccountProof,
  TAccountConfig,
  TAccountFeeReceiver,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? POW_MINER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userAccount: { value: input.userAccount ?? null, isWritable: false },
    proof: { value: input.proof ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    feeReceiver: { value: input.feeReceiver ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.proof),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.feeReceiver),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getVerifyProofInstructionDataEncoder().encode(
      args as VerifyProofInstructionDataArgs
    ),
  } as VerifyProofInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserAccount,
    TAccountProof,
    TAccountConfig,
    TAccountFeeReceiver,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedVerifyProofInstruction<
  TProgram extends string = typeof POW_MINER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userAccount: TAccountMetas[1];
    proof: TAccountMetas[2];
    config: TAccountMetas[3];
    feeReceiver: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: VerifyProofInstructionData;
};

export function parseVerifyProofInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedVerifyProofInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userAccount: getNextAccount(),
      proof: getNextAccount(),
      config: getNextAccount(),
      feeReceiver: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getVerifyProofInstructionDataDecoder().decode(instruction.data),
  };
}
