/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import { findProgramStatePda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RenewSubscriptionInstructionAccounts = {
  user?: PublicKey | Pda;
  escrow?: PublicKey | Pda;
  master?: PublicKey | Pda;
  masterSigner: Signer;
  recipient: PublicKey | Pda;
  authority?: PublicKey | Pda;
  feeProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  programState?: PublicKey | Pda;
};

// Data.
export type RenewSubscriptionInstructionData = {
  discriminator: Uint8Array;
  feeAmount: bigint;
};

export type RenewSubscriptionInstructionDataArgs = {
  feeAmount: number | bigint;
};

export function getRenewSubscriptionInstructionDataSerializer(): Serializer<
  RenewSubscriptionInstructionDataArgs,
  RenewSubscriptionInstructionData
> {
  return mapSerializer<
    RenewSubscriptionInstructionDataArgs,
    any,
    RenewSubscriptionInstructionData
  >(
    struct<RenewSubscriptionInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['feeAmount', u64()],
      ],
      { description: 'RenewSubscriptionInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([45, 75, 154, 194, 160, 10, 111, 183]),
    })
  ) as Serializer<
    RenewSubscriptionInstructionDataArgs,
    RenewSubscriptionInstructionData
  >;
}

// Args.
export type RenewSubscriptionInstructionArgs =
  RenewSubscriptionInstructionDataArgs;

// Instruction.
export function renewSubscription(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: RenewSubscriptionInstructionAccounts & RenewSubscriptionInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'useraccount',
    'KoNT93eGXFcQJxZQqBvrBLQQCcxEPhKrmZoqFNmxGjk'
  );

  // Accounts.
  const resolvedAccounts = {
    user: { index: 0, isWritable: true as boolean, value: input.user ?? null },
    escrow: {
      index: 1,
      isWritable: true as boolean,
      value: input.escrow ?? null,
    },
    master: {
      index: 2,
      isWritable: true as boolean,
      value: input.master ?? null,
    },
    masterSigner: {
      index: 3,
      isWritable: false as boolean,
      value: input.masterSigner ?? null,
    },
    recipient: {
      index: 4,
      isWritable: true as boolean,
      value: input.recipient ?? null,
    },
    authority: {
      index: 5,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    feeProgram: {
      index: 6,
      isWritable: true as boolean,
      value: input.feeProgram ?? null,
    },
    systemProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    programState: {
      index: 8,
      isWritable: true as boolean,
      value: input.programState ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RenewSubscriptionInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity.publicKey;
  }
  if (!resolvedAccounts.user.value) {
    resolvedAccounts.user.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([117, 115, 101, 114])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.escrow.value) {
    resolvedAccounts.escrow.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([101, 115, 99, 114, 111, 119])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.user.value)
      ),
    ]);
  }
  if (!resolvedAccounts.master.value) {
    resolvedAccounts.master.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 115, 116, 101, 114])),
    ]);
  }
  if (!resolvedAccounts.feeProgram.value) {
    resolvedAccounts.feeProgram.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([102, 101, 101, 95, 112, 114, 111, 103, 114, 97, 109])
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.programState.value) {
    resolvedAccounts.programState.value = findProgramStatePda(context);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRenewSubscriptionInstructionDataSerializer().serialize(
    resolvedArgs as RenewSubscriptionInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
