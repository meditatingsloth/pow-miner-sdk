/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { findProgramStatePda } from '../pdas';
import { USERACCOUNT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const EXTRACT_FEE_DISCRIMINATOR = new Uint8Array([
  163, 228, 222, 41, 50, 218, 177, 34,
]);

export function getExtractFeeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXTRACT_FEE_DISCRIMINATOR);
}

export type ExtractFeeInstruction<
  TProgram extends string = typeof USERACCOUNT_PROGRAM_ADDRESS,
  TAccountUser extends string | IAccountMeta<string> = string,
  TAccountMaster extends string | IAccountMeta<string> = string,
  TAccountMasterSigner extends string | IAccountMeta<string> = string,
  TAccountRecipient extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountFeeProgram extends string | IAccountMeta<string> = string,
  TAccountProgramState extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableAccount<TAccountUser>
        : TAccountUser,
      TAccountMaster extends string
        ? WritableAccount<TAccountMaster>
        : TAccountMaster,
      TAccountMasterSigner extends string
        ? ReadonlySignerAccount<TAccountMasterSigner> &
            IAccountSignerMeta<TAccountMasterSigner>
        : TAccountMasterSigner,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountAuthority extends string
        ? WritableAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountFeeProgram extends string
        ? WritableAccount<TAccountFeeProgram>
        : TAccountFeeProgram,
      TAccountProgramState extends string
        ? WritableAccount<TAccountProgramState>
        : TAccountProgramState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ExtractFeeInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type ExtractFeeInstructionDataArgs = { amount: number | bigint };

export function getExtractFeeInstructionDataEncoder(): Encoder<ExtractFeeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: EXTRACT_FEE_DISCRIMINATOR })
  );
}

export function getExtractFeeInstructionDataDecoder(): Decoder<ExtractFeeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getExtractFeeInstructionDataCodec(): Codec<
  ExtractFeeInstructionDataArgs,
  ExtractFeeInstructionData
> {
  return combineCodec(
    getExtractFeeInstructionDataEncoder(),
    getExtractFeeInstructionDataDecoder()
  );
}

export type ExtractFeeAsyncInput<
  TAccountUser extends string = string,
  TAccountMaster extends string = string,
  TAccountMasterSigner extends string = string,
  TAccountRecipient extends string = string,
  TAccountAuthority extends string = string,
  TAccountFeeProgram extends string = string,
  TAccountProgramState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user?: Address<TAccountUser>;
  master?: Address<TAccountMaster>;
  masterSigner: TransactionSigner<TAccountMasterSigner>;
  recipient: Address<TAccountRecipient>;
  authority: Address<TAccountAuthority>;
  feeProgram?: Address<TAccountFeeProgram>;
  programState?: Address<TAccountProgramState>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: ExtractFeeInstructionDataArgs['amount'];
};

export async function getExtractFeeInstructionAsync<
  TAccountUser extends string,
  TAccountMaster extends string,
  TAccountMasterSigner extends string,
  TAccountRecipient extends string,
  TAccountAuthority extends string,
  TAccountFeeProgram extends string,
  TAccountProgramState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof USERACCOUNT_PROGRAM_ADDRESS,
>(
  input: ExtractFeeAsyncInput<
    TAccountUser,
    TAccountMaster,
    TAccountMasterSigner,
    TAccountRecipient,
    TAccountAuthority,
    TAccountFeeProgram,
    TAccountProgramState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ExtractFeeInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountMaster,
    TAccountMasterSigner,
    TAccountRecipient,
    TAccountAuthority,
    TAccountFeeProgram,
    TAccountProgramState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? USERACCOUNT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    master: { value: input.master ?? null, isWritable: true },
    masterSigner: { value: input.masterSigner ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    feeProgram: { value: input.feeProgram ?? null, isWritable: true },
    programState: { value: input.programState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.user.value) {
    accounts.user.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([117, 115, 101, 114])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }
  if (!accounts.master.value) {
    accounts.master.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([109, 97, 115, 116, 101, 114])),
      ],
    });
  }
  if (!accounts.feeProgram.value) {
    accounts.feeProgram.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([102, 101, 101, 95, 112, 114, 111, 103, 114, 97, 109])
        ),
      ],
    });
  }
  if (!accounts.programState.value) {
    accounts.programState.value = await findProgramStatePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.master),
      getAccountMeta(accounts.masterSigner),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.feeProgram),
      getAccountMeta(accounts.programState),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getExtractFeeInstructionDataEncoder().encode(
      args as ExtractFeeInstructionDataArgs
    ),
  } as ExtractFeeInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountMaster,
    TAccountMasterSigner,
    TAccountRecipient,
    TAccountAuthority,
    TAccountFeeProgram,
    TAccountProgramState,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ExtractFeeInput<
  TAccountUser extends string = string,
  TAccountMaster extends string = string,
  TAccountMasterSigner extends string = string,
  TAccountRecipient extends string = string,
  TAccountAuthority extends string = string,
  TAccountFeeProgram extends string = string,
  TAccountProgramState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: Address<TAccountUser>;
  master: Address<TAccountMaster>;
  masterSigner: TransactionSigner<TAccountMasterSigner>;
  recipient: Address<TAccountRecipient>;
  authority: Address<TAccountAuthority>;
  feeProgram: Address<TAccountFeeProgram>;
  programState: Address<TAccountProgramState>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: ExtractFeeInstructionDataArgs['amount'];
};

export function getExtractFeeInstruction<
  TAccountUser extends string,
  TAccountMaster extends string,
  TAccountMasterSigner extends string,
  TAccountRecipient extends string,
  TAccountAuthority extends string,
  TAccountFeeProgram extends string,
  TAccountProgramState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof USERACCOUNT_PROGRAM_ADDRESS,
>(
  input: ExtractFeeInput<
    TAccountUser,
    TAccountMaster,
    TAccountMasterSigner,
    TAccountRecipient,
    TAccountAuthority,
    TAccountFeeProgram,
    TAccountProgramState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ExtractFeeInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountMaster,
  TAccountMasterSigner,
  TAccountRecipient,
  TAccountAuthority,
  TAccountFeeProgram,
  TAccountProgramState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? USERACCOUNT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    master: { value: input.master ?? null, isWritable: true },
    masterSigner: { value: input.masterSigner ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    feeProgram: { value: input.feeProgram ?? null, isWritable: true },
    programState: { value: input.programState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.master),
      getAccountMeta(accounts.masterSigner),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.feeProgram),
      getAccountMeta(accounts.programState),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getExtractFeeInstructionDataEncoder().encode(
      args as ExtractFeeInstructionDataArgs
    ),
  } as ExtractFeeInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountMaster,
    TAccountMasterSigner,
    TAccountRecipient,
    TAccountAuthority,
    TAccountFeeProgram,
    TAccountProgramState,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedExtractFeeInstruction<
  TProgram extends string = typeof USERACCOUNT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    master: TAccountMetas[1];
    masterSigner: TAccountMetas[2];
    recipient: TAccountMetas[3];
    authority: TAccountMetas[4];
    feeProgram: TAccountMetas[5];
    programState: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: ExtractFeeInstructionData;
};

export function parseExtractFeeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedExtractFeeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      master: getNextAccount(),
      masterSigner: getNextAccount(),
      recipient: getNextAccount(),
      authority: getNextAccount(),
      feeProgram: getNextAccount(),
      programState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getExtractFeeInstructionDataDecoder().decode(instruction.data),
  };
}
