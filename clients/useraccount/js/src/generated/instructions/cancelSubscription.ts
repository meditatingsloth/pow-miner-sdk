/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { findProgramStatePda } from '../pdas';
import { USERACCOUNT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CANCEL_SUBSCRIPTION_DISCRIMINATOR = new Uint8Array([
  60, 139, 189, 242, 191, 208, 143, 18,
]);

export function getCancelSubscriptionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_SUBSCRIPTION_DISCRIMINATOR
  );
}

export type CancelSubscriptionInstruction<
  TProgram extends string = typeof USERACCOUNT_PROGRAM_ADDRESS,
  TAccountUser extends string | IAccountMeta<string> = string,
  TAccountEscrow extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TAccountProgramState extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableAccount<TAccountUser>
        : TAccountUser,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountProgramState extends string
        ? WritableAccount<TAccountProgramState>
        : TAccountProgramState,
      ...TRemainingAccounts,
    ]
  >;

export type CancelSubscriptionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelSubscriptionInstructionDataArgs = {};

export function getCancelSubscriptionInstructionDataEncoder(): Encoder<CancelSubscriptionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_SUBSCRIPTION_DISCRIMINATOR })
  );
}

export function getCancelSubscriptionInstructionDataDecoder(): Decoder<CancelSubscriptionInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelSubscriptionInstructionDataCodec(): Codec<
  CancelSubscriptionInstructionDataArgs,
  CancelSubscriptionInstructionData
> {
  return combineCodec(
    getCancelSubscriptionInstructionDataEncoder(),
    getCancelSubscriptionInstructionDataDecoder()
  );
}

export type CancelSubscriptionAsyncInput<
  TAccountUser extends string = string,
  TAccountEscrow extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountProgramState extends string = string,
> = {
  user?: Address<TAccountUser>;
  escrow?: Address<TAccountEscrow>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  programState?: Address<TAccountProgramState>;
};

export async function getCancelSubscriptionInstructionAsync<
  TAccountUser extends string,
  TAccountEscrow extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountProgramState extends string,
  TProgramAddress extends Address = typeof USERACCOUNT_PROGRAM_ADDRESS,
>(
  input: CancelSubscriptionAsyncInput<
    TAccountUser,
    TAccountEscrow,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountProgramState
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CancelSubscriptionInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountEscrow,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountProgramState
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? USERACCOUNT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    programState: { value: input.programState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.user.value) {
    accounts.user.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([117, 115, 101, 114])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }
  if (!accounts.escrow.value) {
    accounts.escrow.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([101, 115, 99, 114, 111, 119])),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.programState.value) {
    accounts.programState.value = await findProgramStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.programState),
    ],
    programAddress,
    data: getCancelSubscriptionInstructionDataEncoder().encode({}),
  } as CancelSubscriptionInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountEscrow,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountProgramState
  >;

  return instruction;
}

export type CancelSubscriptionInput<
  TAccountUser extends string = string,
  TAccountEscrow extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountProgramState extends string = string,
> = {
  user: Address<TAccountUser>;
  escrow: Address<TAccountEscrow>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  programState: Address<TAccountProgramState>;
};

export function getCancelSubscriptionInstruction<
  TAccountUser extends string,
  TAccountEscrow extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountProgramState extends string,
  TProgramAddress extends Address = typeof USERACCOUNT_PROGRAM_ADDRESS,
>(
  input: CancelSubscriptionInput<
    TAccountUser,
    TAccountEscrow,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountProgramState
  >,
  config?: { programAddress?: TProgramAddress }
): CancelSubscriptionInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountEscrow,
  TAccountAuthority,
  TAccountSystemProgram,
  TAccountProgramState
> {
  // Program address.
  const programAddress = config?.programAddress ?? USERACCOUNT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    programState: { value: input.programState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.programState),
    ],
    programAddress,
    data: getCancelSubscriptionInstructionDataEncoder().encode({}),
  } as CancelSubscriptionInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountEscrow,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountProgramState
  >;

  return instruction;
}

export type ParsedCancelSubscriptionInstruction<
  TProgram extends string = typeof USERACCOUNT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    escrow: TAccountMetas[1];
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
    programState: TAccountMetas[4];
  };
  data: CancelSubscriptionInstructionData;
};

export function parseCancelSubscriptionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedCancelSubscriptionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      escrow: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
      programState: getNextAccount(),
    },
    data: getCancelSubscriptionInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
